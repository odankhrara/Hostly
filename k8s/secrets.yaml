apiVersion: v1
kind: Secret
metadata:
  name: hostly-secrets
  namespace: hostly
type: Opaque
stringData:
  # Database password - UPDATE THIS IN PRODUCTION
  DB_PASSWORD: ""
  # Session secret - UPDATE THIS IN PRODUCTION
  SESSION_SECRET: "your-super-secret-session-key-here-change-in-production"
  # API Keys - UPDATE THESE IN PRODUCTION
  OPENAI_API_KEY: ""
  TAVILY_API_KEY: ""

---
# Note: In production, use sealed-secrets, external-secrets, or cloud provider secret management
# Example for kubectl:
# kubectl create secret generic hostly-secrets \
#   --from-literal=DB_PASSWORD='your-password' \
#   --from-literal=SESSION_SECRET='your-secret' \
#   --from-literal=OPENAI_API_KEY='your-key' \
#   --from-literal=TAVILY_API_KEY='your-key' \
#   -n hostly

